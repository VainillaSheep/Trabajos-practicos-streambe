const texto = prompt("Por favor, ingresa un texto breve:");
const caracteres = texto.length;
console.log(`El texto ingresado es: ${texto}`);
console.log(`La cantidad de caracteres es: ${caracteres}`);